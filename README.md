This project is a simple Python-based QR Code Generator using the qrcode library.
It converts any URL or text into a QR code image and saves it automatically.

📌 Features

Generate QR codes from any text or URL

Customize version, box size, and border

Saves the QR code as an image (.png)

📂 Installation

Install the required library:

pip install qrcode
pip install image

▶️ How to Run

Save the Python file (example: qrcodegenerator.py)

Run the script:

python qrcodegenerator.py


A file named test.png will be created in the same folder.
